<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.kaixue.manage_course.dao.TeachPlanMapper">
    <resultMap id="teachPlanMap" type="com.kaixue.domain.course.ext.TeachPlanNode">
        <id column="one_id" property="id"/>
        <result column="one_name" property="pName"/>
        <collection property="children" ofType="com.kaixue.domain.course.ext.TeachPlanNode">
            <id column="two_id" property="id"/>
            <result column="two_name" property="pName"/>
            <collection property="children" ofType="com.kaixue.domain.course.ext.TeachPlanNode">
                <id column="three_id" property="id"/>
                <result column="three_name" property="pName"/>
            </collection>
        </collection>
    </resultMap>
    <select id="findList" parameterType="java.lang.String" resultMap="teachPlanMap">
        SELECT
        a.id one_id,
        a.p_name one_name,
        b.id two_id,
        b.p_name two_name,
        c.id three_id,
        c.p_name three_name
        FROM
        teach_plan a
        LEFT JOIN teach_plan b
        ON b.parent_id = a.id
        LEFT JOIN teach_plan c
        ON c.parent_id = b.id
        WHERE a.parent_id = '0'
        <if test="_parameter !=null and _parameter!=''">
            AND a.course_id = #{courseId}
        </if>
        ORDER BY a.order_by,
        b.order_by,
        c.order_by
    </select>
</mapper>